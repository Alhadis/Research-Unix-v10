the enclosed c file for the mac32, preprocessed and all, generates:
Assembler: x.s
	aline 341(cline 4): Unbalanced Symbol Table Entries-Too Many Scope Beginnings
when compiled comp<x.i.  without -g tables (comp -lg < x.i ?), it works.

-rob
