Check README in dped, hped, and tped

The files in doc should be copied into the directory
named in Unix.h #define DOC_LIB (notice that the DOC in that 
string is part of the basename of the file, not the directory)
