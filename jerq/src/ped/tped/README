tped is really the host part of ped with different low
level routines. Many of the *.o files must be linked from
the directory of the host part of ped.

Unless you have a troff postprocessor that knows about ped you
should run tped with -c flag.
