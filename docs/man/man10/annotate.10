.TH ANNOTATE 10.1 UCDS
.SH NAME
annotate \- annotate .j files with pin numbers
.SH SYNOPSIS
.B annotate
.I net-list
.I net-files
.SH DESCRIPTION
.PP
.I Annotate
takes the pin numbers from the net-list file (output from
.IR cdmglob (10.1))
and the net files (output from
.B "jraw -w" 
and stored in filenames ending in
.BR .w )
and writes the coordinates of the pin numbers
to corresponding files ending with
.BR \&.a .
The only useful thing to do with the
.B \&.a
files is to append the
corresponding
.B \&.j
files to them and print or examine with
.IR jraw (10.1).
.SH SEE ALSO
.IR cdmglob (10.1),
.IR jraw (10.1)
