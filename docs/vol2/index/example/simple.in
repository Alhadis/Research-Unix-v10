This is a simple example with a couple of
citations |sp(|s)reference(awk) |sp(|s)reference(troff).
Citations can be |sp(|s)reference(awk) in the
middle of a line, starting a new line,
|sp(|s)reference(troff)
or flush|sp(|s)reference(awk) against a word.
|sp(|s)reference_placement
