Binaries made here are supposed to live on the console disk,
and be loaded by the boot command file.
In fact they must be disassembled and typed into the command
file by hand as a set of D commands until someone writes the
code to let the VAX write on the console's disk.
