|DEFINE_NUMBER(mercury_counter, M)
|DEFINE_STRING_ARRAY(mercury_array, M)
|DEFINE_STRING(mercury_string, ME)

|DEFINE_STRING(earlier_document_number_string, EN)

|DEFINE_STRING(att_release_string, AR)

|DEFINE_STRING(proprietary_string, PC)
|DEFINE_STRING(government_string, GS)
|DEFINE_STRING(dh_string, DH)
|DEFINE_STRING(director_string, DN)
|DEFINE_STRING(complete_memo_string, CM)
|DEFINE_STRING(cover_sheet_string, CS)

|DEFINE_STRING(plural_string, SS)

|DEFINE_NUMBER(other_marker, OM)

|DEFINE_NUMBER(other_counter, OC)
|DEFINE_NUMBER(text_counter, TP)
|DEFINE_NUMBER(total_counter, TT)

|DEFINE_NUMBER(more_abstract, MA)
|DEFINE_NUMBER(more_completememo, MM)
|DEFINE_NUMBER(more_coversheet, MS)

|DEFINE_NUMBER(position1, P1)

|DEFINE_NUMBER(i_plus_one, i)
|DEFINE_NUMBER(i_plus_two, j)
|DEFINE_NUMBER(i_plus_three, k)
