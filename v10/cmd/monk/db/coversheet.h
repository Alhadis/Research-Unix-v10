|REGISTER(mercury_counter, M)
|STRING_ARRAY(mercury_array, M)
|STRING(mercury_string, ME)

|STRING(earlier_document_number_string, EN)

|STRING(att_release_string, AR)

|STRING(proprietary_string, PC)
|STRING(government_string, GS)
|STRING(dh_string, DH)
|STRING(director_string, DN)
|STRING(complete_memo_string, CM)
|STRING(cover_sheet_string, CS)

|STRING(plural_string, SS)

|REGISTER(other_marker, OM)

|REGISTER(other_counter, OC)
|REGISTER(text_counter, TP)
|REGISTER(total_counter, TT)

|REGISTER(more_abstract, MA)
|REGISTER(more_completememo, MM)
|REGISTER(more_coversheet, MS)

|REGISTER(position1, P1)

|REGISTER(i_plus_one, i)
|REGISTER(i_plus_two, j)
|REGISTER(i_plus_three, k)
