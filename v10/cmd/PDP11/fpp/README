Floating point preprocessor, made to work with the -F option in 11cc.

Needs F library routines: must select this library on the 11cc command
line before the standard C library which uses fpp for its floating point:
that means that 11libc.a can't be used, instead use 11libFc.a.
