This hierarchy is the distribution for lcc.  The version number is
given in ./c/version.h, and ./LOG describes the changes since the
last release.

A complete description of the distribution and installation
instructions are given in ./doc/install.ps, which is the PostScript
generated from the LaTeX document ./doc/install.tex.
