/* dioden.f -- translated by f2c (version of 5 February 1993  1:37:14).
   You must link the resulting object file with the libraries:
	-lF77 -lI77 -lm -lc   (in that order)
*/

#include "f2c.h"

/* Subroutine */ int dioden_(nv, t, dt, vold, v, vt)
integer *nv;
doublereal *t, *dt, *vold, *v, *vt;
{
/* THE DEFAULT NEWTON ITERATION INITIALIZER FOR IODES. */
    /* Parameter adjustments */
    --vt;
    --v;
    --vold;

    /* Function Body */
    return 0;
} /* dioden_ */

