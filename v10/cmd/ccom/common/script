1631,1641d
1625c
	    !asgop(p->in.op))
.
1622c
	/* don't push a conversion down the lhs of an assign op */
.
1512,1515d
554a
				if (p->fn.type == FLOAT)
					p->fn.type = p->fn.csiz = DOUBLE;
.
w
q
