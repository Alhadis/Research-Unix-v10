/*LINTLIBRARY*/
#include <ipc.h>
char *ipcpath(h, n, s) char *h, *n, *s; { return ""; }
int ipcopen(s, p) char *s, *p; { return 1; }
int ipccreat(s, p) char *s, *p; { return 1; }
ipcinfo *ipclisten(fd) { return ipclisten(fd); }
int ipcaccept(i) ipcinfo *i; { return 1; }
int ipcreject(i, n, s) ipcinfo *i; char *s; { return 1; }
int ipcexec(s, p, c) char *s, *p, *c; { return 1; }
char *errstr;
